{
  "name": "tesseract",
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "apps/*",
    "schema"
  ],
  "scripts": {
    "dev:cloud": "cd apps/cloud-console && npm run dev",
    "build:cloud": "cd apps/cloud-console && npm run build",
    "build:all": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.48"
  }
}
